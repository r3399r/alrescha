import IcLoading from 'src/image/ic-loading.svg';

const Loading = () => (
  <div className="w-full h-[320px] bg-grey-300 flex items-center justify-center">
    <img src={IcLoading} />
  </div>
);

export default Loading;
